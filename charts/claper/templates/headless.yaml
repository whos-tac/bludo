apiVersion: v1
kind: Service
metadata:
  name: bludo-svc-headless
spec:
  ports:
    - port: 4369
      targetPort: epmd
      protocol: TCP
      name: epmd
  selector:
    app: bludo
  clusterIP: None